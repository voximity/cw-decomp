[package]
name = "cw-net"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Bit-exact packet encoding of the Cube World Alpha protocol: byte buffers, entity deltas, packet layouts"

[dependencies]
flate2 = "1"

[dev-dependencies]
sha2 = "0.10"
hex = "0.4"
